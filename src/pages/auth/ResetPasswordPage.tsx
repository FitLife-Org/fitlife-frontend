export default function ResetPasswordPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-950 px-4">
      <section className="w-full max-w-md rounded-lg bg-white p-8 shadow-xl">
        <h1 className="text-3xl font-black text-slate-950">Đặt lại mật khẩu</h1>
        <p className="mt-2 text-sm text-slate-500">Màn hình này đã sẵn cấu trúc để kết nối API reset password.</p>
      </section>
    </main>
  );
}
